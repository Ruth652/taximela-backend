services:
  - type: web
    name: taximela-otp
    env: docker
    dockerfilePath: ./Dockerfile.otp
    plan: starter
    region: oregon
    envVars:
      - key: JAVA_OPTS
        value: "-Xmx1G -server"
    healthCheckPath: /otp/routers/default